import { List, ListItem, styled } from "@mui/material";

const commonStyles = {
  padding: 0,
};

export const CustomList = styled(List)(commonStyles);
export const CustomListItem = styled(ListItem)(commonStyles);
