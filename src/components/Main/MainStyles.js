import { Box, styled } from "@mui/material";

export const CustomMain = styled(Box)({
  display: "flex",
  flexDirection: "column",
  alignItems: "center",
  marginTop: "12vh",
});

export const CustomBox = styled(Box)({
  marginBottom: "38px",
});

export const CustomSearchWrapper = styled(CustomBox)({
  width: "100%",
});